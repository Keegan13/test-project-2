import * as React from 'react';

export interface ChatProps {
}

export const Chat = (props: ChatProps) =>
 <h1>This is react app written with typescript 
     served from asp net core site with webpack's hot module replacement
     without typescipt compiler (bable typescript preset)</h1>;

export default Chat;
